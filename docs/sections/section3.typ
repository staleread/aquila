= РОЗДІЛ 3. ЕКСПЕРИМЕНТАЛЬНЕ ДОСЛІДЖЕННЯ ТА ПРОГРАМНА РЕАЛІЗАЦІЯ КРИПТОСИСТЕМИ

== 3.1. Структура та інтерфейс програмного комплексу

=== 3.1.1. Організація консольного інтерфейсу (CLI) та порядок роботи

Для забезпечення можливості масової генерації тестових даних, автоматизації процесів алгебраїчного криптоаналізу та безпосередньої експлуатації розробленої бібліотеки, було реалізовано консольну утиліту #box[`aquila-cli`]. Взаємодія з користувачем (дослідником) будується на основі системи підкоманд, кожна з яких відповідає за ізольований етап життєвого циклу криптосистеми.

Порядок роботи із системою передбачає таку послідовність кроків:
1. *Перевірка поточної конфігурації (`config`):* Відображає параметри клітинного автомата, зафіксовані на етапі компіляції (`block size`, `compositions count`, `fold size`, `algebraic degree`).
2. *Генерація пари ключів (`gen`):* Створює файли приватного (`id_aquila[M]`) та публічного (`id_aquila[M].pub`) ключів відповідно до активної розрядності.
3. *Шифрування та дешифрування файлів (`enc` / `dec`):* Виконує пряме та зворотне криптоперетворення над довільними бінарними файлами із застосуванням стандартизованого доповнення блоку за архітектурою PKCS\#7.
4. *Символьний експорт (`anf`):* Проводить повну еволюційну згортку та експортує поліноми відкритого ключа у вигляді системи булевих рівнянь в алгебраїчній нормальній формі (АНФ) для подальшого криптоаналізу.
5. *Статистичний аналіз (`analyze`):* Запускає комплекс внутрішніх тестів для оцінки лавинного ефекту та розрахунку кореляційних матриць.

=== 3.1.2. Механізм гарячої перекомпіляції та конфігурування за допомогою Build Tags

Для забезпечення високої гнучкості досліджень без втрати швидкодії, в утиліту інтегровано підкоманду `build`. Цей механізм дозволяє виконувати гарячу перекомпіляцію бінарного файлу «на льоту» під будь-яку необхідну експериментальну конфігурацію. 

Наприклад, виклик команди `aquila-cli build 8c1f8d2` ініціює системний виклик компілятора Go з передачею відповідних `build tags` для збірки тестової 8-бітної версії шифру. Це дозволяє досліднику миттєво переходити від повнорозмірних моделей (96 чи 128 біт) до зменшених (_baby_) реалізацій шифру, що є базовою передумовою для проведення практичного алгебраїчного криптоаналізу.

=== 3.1.3. Схема розгортання та архітектура ресурсів системи

Відповідно до вимог проектування, конфігурація системи з точки зору засобів зберігання та обробки даних, а також взаємозв'язок між модулями автоматизації експерименту та ядром криптосистеми, описується схемою розгортання (ресурсів системи). Основні обчислювальні вузли включають локальну робочу станцію дослідника, що керує процесом компіляції, та ізольовані контейнеризовані середовища (Docker) для запуску зовнішніх SAT/ANF розв'язувачів.

== 3.2. Стратегія та покриття тестуванням (Unit Testing)

Для забезпечення абсолютної математичної коректності та верифікації криптографічних примітивів на кожному рівні абстракції, програмний комплекс покрито розгалуженою системою модульних тестів (_unit tests_). Організація процесу тестування має такі специфічні особливості:
- *Умовна ізоляція тестів:* Тестові сценарії використовують директиви умовної збірки (`//go:build block32`), що дозволяє запускати перевірки виключно для типів даних поточної активної розрядності блоку.
- *Детермінована псевдовипадковість:* Усі тести, що потребують випадкових початкових умов (генерація матриць чи ключів), використовують генератор псевдовипадкових чисел із фіксованим початковим значенням (_seed_): `rand.New(rand.NewSource(42))`. Це гарантує відтворюваність результатів тестів при повторних запусках.
- *Табличний підхід:* Перевірка математичних функцій реалізована у вигляді табличних тестів (_table-driven tests_), що спрощує покриття граничних умов.

Структура покриття тестами розділена на чотири логічні рівні:

1. *Рівень представлення станів (`aquila/state`):* Верифікує побітові операції над машинними словами, що репрезентують стан КА (методи встановлення/читання бітів `SetAt`, `At`). Round-trip тести перевіряють коректність серіалізації/десеріалізації станів у бінарні буфери (`Read`/`Write`). Також покриваються логічні відношення: побітове об'єднання (`Or`), лексикографічне порівняння (`Compare`), перевірка підмножини маски (`Contains`) та агрегація індексів активних бітів (`Subscripts`).

2. *Рівень символьної математики та лінійної алгебри (`aquila/math`):* Тест `TestSLEEvalSolve` генерує випадкову матрицю СЛАР і перевіряє, що для довільного вхідного вектора $x$ операції прямого обчислення $y = text("SLE.Eval")(x)$ та зворотного розв'язання $x' = text("SLE.Solve")(y)$ через $L U$-підстановку дають абсолютно тотожний результат ($x == x'$). На рівні поліномів тестується правильність розкриття дужок (дистрибутивність) при множенні та ефект повного взаємознищення однакових мономів у полі $FF_2$ ($x xor x = 0$).

3. *Рівень клітинних автоматів (`aquila/crypto/invertible` та `general`):* Тест `TestCAInvertibility` підтверджує успішність розшифрування випадкових блоків даних приватним ключем після прямого перетворення. Тест `TestDeriveGeneralCA` верифікує коректність процесу символьної деривації: він порівнює результати шифрування набору даних на приватному КА та на отриманій з нього системі поліномів відкритого ключа, гарантуючи відсутність похибок згортки.

4. *Асиметричний криптографічний API (`aquila/asym`):* Високорівневі сценарії `TestEncryptDecrypt` та `TestSignVerify` підтверджують працездатність системи в режимах асиметричного шифрування та електронного цифрового підпису. Стійкість ЕЦП перевіряється інверсією 1 біта у тілі підпису, що має гарантовано викликати повернення значення `false` функцією верифікації.

== 3.3. Результати експериментальних досліджень

Для комплексного дослідження властивостей розробленого примітиву було розроблено інструментальний комплекс автоматизації експериментів. Комплекс забезпечує автоматичну циклічну перекомпіляцію `aquila-cli`, масову генерацію ключів, експорт систем рівнянь АНФ та запуск SAT/ANF-розв'язувача `Bosphorus` всередині ізольованих Docker-контейнерів з фіксацією часових метрик.

=== 3.3.1. Оцінка лавинного ефекту (SAC)

В межах аналізу криптографічних властивостей було проведено дослідження лавинного ефекту (Strict Avalanche Criterion) під впливом зміни конструктивних параметрів моделі. Результати експериментів зафіксовано в локальних таблицях `avalanche.csv` та візуалізовано на відповідних графіках. 

Аналіз отриманих даних дозволив виявити такі закономірності:
- *Вплив алгебраїчного степеня $d$:* Перехід від степеня нелінійності $d=2$ до $d=3$ призводить до значного наближення показників середньої кількості змінених вихідних бітів до ідеальної межі $N/2$ (де $N$ -- розмір блоку). Спостерігається суттєве зменшення амплітуди коливань (девіації) лавинної характеристики.
- *Вплив розміру шару СЛАР $f$:* Масштабування розміру лінійного дифузійного блоку ($f in \{8, 16, 32\}$) різко покращує зв'язність бітів системи. Це забезпечує високу рівномірність поширення змін по всій довжині криптографічного блоку.
- *Вплив кількості раундів каскадування $c$:* Додавання другого раунду композиції ($c=2$) призводить до практично повного згладжування девіацій характеристики лавинного ефекту. Графік середньої зміни трансформується у майже строгу горизонтальну лінію на рівні $N/2$, що свідчить про досягнення високої якості дифузії в структурі шифру.

=== 3.3.2. Кореляційний аналіз щільності мономів

За результатами аналізу даних з файлів `monom-correlation.csv` було досліджено взаємозв'язок між кількістю мономів у АНФ вихідних поліномів публічного ключа (алгебраїчною складністю) та статистичною ймовірністю лавинної зміни відповідного вихідного біта.

Експериментально доведено, що вихідні координатні функції, які мають малу кількість нелінійних мономів, характеризуються високим рівнем девіації лавинної ймовірності -- окремі біти змінюються занадто передбачувано або майже не реагують на інверсію входу. Проте у міру кумулятивного накопичення мономів при збільшенні параметрів $d$ та $c$, ймовірність зміни кожного вихідного біта асимптотично наближається до ідеального значення $0.5$, а лінійний тренд стабілізується.

=== 3.3.3. Оцінка меж криптостійкості за допомогою SAT-криптоаналізу

Найважливішою частиною дослідження стало експериментальне визначення меж обчислювальної складності розв'язання поліноміальних систем відкритого ключа за допомогою моделювання практичних алгебраїчних атак. Часові метрики успішних атак розв'язувача `Bosphorus` зведено у таблиці `sat-results.csv` та `sat-success.csv`.

Розподіл часу виконання атак на зменшених моделях та динаміка частки успішних зламів (_Success Rate_) дозволили зафіксувати чіткий криптографічний стрибок стійкості:
1. *Для 16-бітних конфігурацій:* Через обмежений простір станів та мінімальний розмір ключа, розв'язувач легко знаходить рішення булевих рівнянь. Частка успішних атак коливається в межах від $0.42$ до $0.90$, оскільки у міру зростання взаємозв'язку бітів при збільшенні $d$ та $c$ система стає більш детермінованою для евристик SAT-солвера.
2. *Для 32-бітних конфігурацій:* Експериментально зафіксовано різке падіння ефективності алгебраїчного аналізу при зміні внутрішньої структури. Перехід від конфігурації зі СЛАР розміром 8 (`32c1f8d2`) до конфігурації зі СЛАР розміром 16 (`32c1f16d2`) призводить до обвального падіння показника `Success Rate` з $0.48 / 0.70$ до екстримально низьких значень $0.08 / 0.12$ відповідно, а час роботи розв'язувача починає зростати експоненціально.

Це дослідження практично доводить головну науково-інженерну гіпотезу роботи: збільшення розрядності лінійного дифузійного шару (розміру блоку матриці згортку) є ключовим і вирішальним фактором забезпечення стійкості мультиваріативної криптосистеми проти алгебраїчних атак на основі SAT-solving. Комбінаторний вибух щільності мономів успішно виступає як одностороння функція-пастка, роблячи злам повнорозмірних конфігурацій (96 та 128 біт) обчислювально неможливим за допомогою сучасних математичних розв'язувачів.

== Висновки до розділу 3

У третьому розділі кваліфікаційної роботи проведено комплексне програмне тестування, розгортання та експериментальне дослідження розробленого криптографічного примітиву за допомогою інструментального комплексу автоматизації. За результатами практичного етапу сформульовано такі висновки:

1. Спроектовано та успішно впроваджено консольний додаток `aquila-cli` з підтримкою гарячої перекомпіляції параметрів розрядності через `build tags`. Це дозволило оптимізувати процес дослідження меж обчислювальної складності шифру шляхом миттєвої генерації зменшених (_baby_) копій алгоритму для проведення експериментів.

2. Створено та реалізовано повну інфраструктуру модульного тестування (_unit testing_) на базі табличних тестів та детермінованої псевдовипадковості. Проведені сценарії перевірок повністю підтвердили 100% оборотність прямого і зворотного криптоперетворень, математичну коректність $L U$-розв'язувача СЛАР, а також тотожність символьної АНФ-деривації публічного ключа базовому приватному КА.

3. Експериментальний аналіз строгого лавинного критерію (SAC) довів, що кумулятивне нарощування алгебраїчного степеня нелінійності до $d=3$ та каскадування двох раундів еволюції ($c=2$) призводить до практично повного згладжування девіацій лавинної характеристики. Показник середньої зміни вихідних бітів асимптотично стабілізується на ідеальній межі $N/2$, що свідчить про високу якість дифузії в структурі шифру.

4. На основі кореляційного аналізу щільності мономів виявлено, що зростання кількості нелінійних членів АНФ-поліномів діє як стабілізатор криптографічних властивостей системи: у міру накопичення мономів ймовірність інверсії кожного вихідного біта наближається до еталонного значення $0.5$.

5. Моделювання практичних алгебраїчних атак із використанням сучасного SAT/ANF-розв'язувача `Bosphorus` всередині ізольованих Docker-контейнерів дозволило зафіксувати чіткий криптографічний стрибок стійкості. Встановлено, що ключовим фактором протидії SAT-solving евристикам є не просто кількість раундів, а саме збільшення розрядності лінійного дифузійного шару (розміру згортку $f$): перехід від $f=8$ до $f=16$ у 32-бітних конфігураціях викликав обвальне падіння частки успішних зламів з $0.70$ до $0.12$, підтверджуючи дію комбінаторного вибуху як надійної односторонньої функції-пастки.

#pagebreak()

= ВИСНОВКИ

У кваліфікаційній роботі розв'язано важливе науково-практичне завдання, що полягає у модифікації, програмній реалізації та комплексному експериментальному дослідженні меж обчислювальної складності асиметричного шифру на основі нелінійних неоднорідних клітинних автоматів в умовах сучасних кіберзагроз постквантового періоду.

Проведена оцінка повноти вирішення поставлених завдань свідчить про їхнє виконання у повному обсязі:
1. Проаналізовано теоретичні засади функціонування мультиваріативних криптосистем (MPKC) та виявлено критичні структурні вади оригінальної $s$-згорткової моделі Пухуа Гуаня -- алгебраїчну однорідність (вразливість тривіального нуля) та існування ізольованих лінійних підпросторів.
2. Розроблено архітектуру та програмну Go-бібліотеку криптоперетворень "Aquila", яка завдяки інтеграції векторів афінного зсуву $V$ та регулярних раундових перестановок бітів за алгоритмом Фішера-Єйтса повністю ліквідувала виявлені дефекти базової моделі.
3. Реалізовано інструментальну консольну утиліту `aquila-cli`, що забезпечила повну автоматизацію процесів масової генерації ключів, шифрування, збору статистичних даних та символьного експорту булевих систем рівнянь.
4. Експериментально досліджено проблему комбінаторного вибуху щільності мономів відкритого ключа та доведено її асимптотичний позитивний вплив на наближення системи до ідеальних показників лавинного ефекту.
5. Проведено серію практичних алгебраїчних атак на зменшених копіях шифру за допомогою розв'язувача `Bosphorus`, що дозволило зафіксувати експоненціальне зростання часових витрат SAT-солвера при збільшенні структурних параметрів моделі.
6. Сформульовано науково-практичні рекомендації щодо оптимізації конструктивних параметрів для досягнення високого ступеня постквантового захисту при масштабуванні блоку до практичних розмірів.

== Науково-практичні рекомендації щодо оптимізації та масштабування моделі

На основі отриманих теоретичних та експериментальних результатів, для досягнення максимального рівня криптостійкості при масштабуванні розробленого примітиву до практичних розмірів блоку в 96 та 128 біт, сформульовано такі рекомендації:

- *Розмірність лінійного шару (Fold Size):* Параметр розрядності згортку $f$ має становити щонайменше 16 або 32 біти. Збільшення розміру блоку лінійної дифузійної матриці $A_i$ є пріоритетним фактором, оскільки воно кардинально ускладнює структуру графів залежностей булевих змінних, роблячи систему нерозв'язною для сучасних SAT-солверів.
- *Алгебраїчний степінь ($d$):* Оптимальним значенням параметра нелінійності для початкових раундів є $d=3$. Використання кумулятивного формування мономів степеня 3 забезпечує швидке досягнення ідеального лавинного ефекту (близькість до $0.5$) вже на другому раунді еволюції, утримуючи при цьому об'єм пам'яті для зберігання коефіцієнтів у межах лімітів вбудованих систем.
- *Кількість раундів ($r$):* Для повнорозмірних конфігурацій (96/128 біт) рекомендовано використовувати $r >= 2$ раундів каскадування з обов'язковим регулярним перемішуванням бітів між згортками.

== Перспективні напрямки подальших досліджень

В якості векторів подальшого розвитку розробленого програмного комплексу та математичної моделі доцільно виділити:
1. *Дослідження стійкості до альтернативних атак:* Проведення комплексного тестування системи проти методів диференційного та лінійного криптоаналізу, а також оцінка стійкості публічного ключа до специфічних алгоритмів обчислення базисів Грьобнера ($F_4, F_5$).
2. *Інтеграція техніки Hash-Consing:* Впровадження механізмів інтернування та глобального кешування унікальних мономів і проміжних поліноміальних підвиразів на етапі мета-компіляції. Це дозволить кардинально зменшити як кількість дублюючих символьних обчислень при композиції великої кількості раундів КА, так і відкриває перспективи для стиснення фінального розміру публічного ключа завдяки усуненню структурної надликовості.
3. *Інтеграція в протоколи вищого рівня:* Розширення API бібліотеки для можливості її інтеграції у сучасні захищені транспортні протоколи (наприклад, TLS або SSH) в качестве гібридного постквантового механізму інкапсуляції ключів (KEM).